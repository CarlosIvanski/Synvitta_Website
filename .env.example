# Backend forms (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=rainara@synvittabiotech.com
SMTP_PASS=
CONTACT_EMAIL=rainara@synvittabiotech.com

# Where to persist contact form submissions (JSONL file, one JSON per line)
# CONTACTS_STORAGE_PATH=/app/data/contacts.jsonl

# Admin interface (basic auth for /admin and /api/admin/contacts)
# IMPORTANT: set strong credentials in production.
ADMIN_USER=admin@example.com
ADMIN_PASS=change_me

# Frontend build (optional - used at build time)
VITE_FORMS_API_URL=https://forms.synvittadiagnostics.com
